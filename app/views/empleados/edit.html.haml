.section-header
  %ol.breadcrumb
    %li
      Administración de Usuarios
    %li
      Editar
    %li.active
      #{@user.email}
.section-body
  .card
    .card-head.style-primary
      .tools
        = link_to empleados_path, class: 'btn btn-flat' do
          %i.glyphicon.glyphicon-arrow-left
          Regresar a lista
        = link_to empleado_path(@user), class: 'btn btn-flat' do
          %i.fa.fa-eye
          Ver
    .card-body
      = render 'form'
